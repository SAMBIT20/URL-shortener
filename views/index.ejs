<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="style.css" />
    <title>EazyShortener</title>
  </head>
  <body>
    <h1>EazyShortener</h1>
    <img src="img/hero.svg" width="450px auto" alt="">
    <h2>Tiny URLs, Huge Impact!</h2>
    <% if (locals.error) { %>
    <p class="error"><%= error %></p>
    <% } %>

    <form action="/" method="post">
      <input type="url" name="url" id="url" placeholder="Shorten your link here..." />
      <input class="submit-btn" type="submit" value="Shorten" />
    </form>

    <div class="short-url">
      <% if (locals.short_url) { %>
      <a target="_blank" href="<%= short_url %>"><%= short_url %> </a>
      <% } %>
    </div>
    <h3>Welcome to sk.lk, the popular URL shortener - made with ❤️ by Sambit</h3>

  </body>
</html>